const $=s=>document.querySelector(s), $$=s=>document.querySelectorAll(s);
$$('.tab').forEach(b=>b.onclick=()=>{$$('.tab').forEach(x=>x.classList.remove('active'));$$('.panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#'+b.dataset.tab).classList.add('active')});
$$('.ot').forEach(b=>b.onclick=()=>{$$('.ot').forEach(x=>x.classList.remove('active'));$$('.out').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#'+b.dataset.o).classList.add('active')});
$('#run').onclick=async()=>{const r=$('#ref').files[0],s=$('#src').files[0];if(!r||!s){$('#status').textContent='Select both images first.';return}const fd=new FormData();fd.append('reference',r);fd.append('source',s);$('#run').disabled=true;$('#status').textContent='Running PixOrb coarse-to-fine registration…';try{const res=await fetch('/api/register',{method:'POST',body:fd});const data=await res.json();if(!res.ok)throw new Error(data.detail||'Registration failed');$('#results').classList.remove('hidden');for(const [k,v] of Object.entries(data.images))$('#'+k).src=v+'?t='+Date.now();renderMetrics(data.metrics);$('#status').textContent='Registration complete — '+(data.metrics.backend||'unknown backend');}catch(e){$('#status').textContent=e.message}finally{$('#run').disabled=false}};
function renderMetrics(m){const items=[['RMSE',fmt(m.rmse_px)+' px'],['Mean error',fmt(m.mean_reprojection_error_px)+' px'],['Final matches',m.final_match_count],['Final verification',pct(m.final_ransac_inlier_ratio)],['Initial inliers',m.initial_ransac_inlier_count],['Candidates',m.candidate_matches],['Overlap',fmt(m.overlap_percent)+'%'],['Spatial entropy',fmt(m.spatial_entropy_normalized)]];$('#cards').innerHTML=items.map(x=>`<div class="card"><div class="k">${x[0]}</div><div class="v">${x[1]}</div></div>`).join('');$('#json').textContent=JSON.stringify(m,null,2)}function fmt(x){return Number(x).toFixed(3)}function pct(x){return (Number(x)*100).toFixed(2)+'%'}
