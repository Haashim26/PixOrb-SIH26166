# Lean dependency set for the web/Docker deployment only.
# Excludes PySide6 (desktop GUI, only used by pixorb/ui/app.py, not by web/main.py)
# to keep the container build fast and avoid unnecessary Qt system dependencies.

numpy>=1.26
scipy>=1.12
opencv-python-headless>=4.9
rasterio>=1.3
Pillow>=10.0
matplotlib>=3.8
torch>=2.0
torchvision>=0.15
kornia>=0.8

-r web/requirements.txt
